# bindstarter
keyboard listener for Wayland/Hyprland that runs a shell command when you type a configured phrase. reads raw keycodes via evdev — layout-independent. triggers are defined in a dict at the top of the script :D
